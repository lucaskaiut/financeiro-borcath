# URL base da API.
# Em dev (Docker), deixe vazio para usar o proxy do Vite, que encaminha
# /api e /sanctum para o nginx.
# Em produção, defina a URL absoluta da API (sem /api):
# VITE_API_BASE_URL=https://api.example.com
VITE_API_BASE_URL=
VITE_APP_NAME="Nox Skeleton"
